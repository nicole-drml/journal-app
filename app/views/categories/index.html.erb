<p><%= notice %></p>
<p><%= alert %></p>

<h1>Categories</h1>

<div>
    <%= link_to "New Category", new_category_path, :id => "link_to_new_category" %>

    <% @categories.each do |category| %>
        <%= link_to category do %>
            <span><%= category.name %> </span> <br>
        <% end %>
    <% end %>
</div>
