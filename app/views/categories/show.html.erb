<h1><%= @category.name %></h1>
<span><%= @category.created_at.to_s.split(" ")[0] %></span>

<%= link_to "Back to categories", categories_path %>
<%= link_to "Rename category", edit_category_path %>
<%= button_to "Delete category", @category, data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete #{@category.name}?" } %>


