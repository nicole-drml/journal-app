<%= link_to @category, :id => "back_button" do %>
    <div>
        <i class="fa-solid fa-arrow-left"></i>
        <span>Back to <%= @category.name %> </span>
    </div>
<% end %>

<h1> Edit Task </h1>

<%= form_with(url: task_path(@category, @task), scope: @task, method: :patch) do |form| %>
    <div class="data_container">
    <%= form.label :name %>
    <%= form.text_field :name, autofocus: "on" %>
    </div>

    <div class="data_container">
    <%= form.label :due %>
    <%= form.datetime_field :due %>
    </div>

    <div class="data_container" id="notes_container">
    <%= form.label :notes %>
    <%= form.text_area :notes %>
    </div>
    </div>
    
    <div id="submit_button" >
        <%= form.submit%>
    </div>

    <% if @task.errors.any? %>
        <div id="errors_container_new_task">
            <ul>
                <% @task.errors.each do |error| %>
                <li><%= error.full_message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>
<% end %>